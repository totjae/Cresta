# Cresta Web UI 명세

## 1. 목적

사용자가 키움 모의투자 기반 Cresta의 감시 종목, 전략, 행동별 실행 권한, 주문·승인, 포지션, Guard 정책과 시스템 상태를 안전하게 관리할 수 있는 Web UI를 정의한다.

`stitch_cresta_ai_intraday_trading_system`의 화면과 디자인 시스템은 시각적 콘셉트 자료로 사용하지만, 제품 요구사항과 상세 명세가 우선한다.

## 2. 적용 범위

- 데스크톱·태블릿·모바일 반응형 Web UI
- 대시보드, 감시종목, 보유 포지션, 승인·주문, AI 판단
- 거래·Guard 설정, 설정 버전과 영향 미리보기
- 비상정지, 재동기화와 시스템 상태
- 이력·감사 로그
- ID·비밀번호·TOTP 로그인과 고위험 행동 재인증

## 3. 상세 명세

### 3.0 로그인과 인증 화면

로그인 흐름:

```text
사용자 ID·비밀번호 입력
→ 서버 검증
→ TOTP 6자리 입력
→ 인증 완료
→ 대시보드 이동
```

| ID | 요구사항 |
| --- | --- |
| UI-AUTH-001 | 미인증 사용자는 로그인 화면 외의 Console 화면과 실시간 연결에 접근할 수 없다. |
| UI-AUTH-002 | 로그인 화면은 사용자 ID, 비밀번호와 TOTP를 요구하며 오류 원인이 ID·비밀번호·TOTP 중 무엇인지 구분해 노출하지 않는다. |
| UI-AUTH-003 | TOTP 입력은 숫자 6자리, 붙여넣기와 모바일 숫자 키패드를 지원하되 값을 로그·URL·브라우저 저장소에 보존하지 않는다. |
| UI-AUTH-004 | 로그인 실패·잠금·만료 상태는 안전한 일반 메시지와 다시 시도 가능한 시점만 표시한다. |
| UI-AUTH-005 | 세션 만료 시 보호된 내용을 숨기고 로그인 화면으로 이동하며 작성 중이던 주문·승인 요청을 자동 재실행하지 않는다. |
| UI-AUTH-006 | 주문 승인·위험 한도 완화·비상정지 해제 등은 별도 TOTP 재인증 모달을 사용하고 대상 행동을 명시한다. |
| UI-AUTH-007 | 로그아웃은 서버 세션과 실시간 연결을 종료하고 로그인 화면으로 이동한다. |

세부 인증·세션·복구 정책은 [인증 및 보안 명세](SECURITY_SPEC.md)를 따른다.

### 3.1 디자인 기준

재사용할 콘셉트:

- 깊은 네이비 기반 다크 모드
- 얇은 경계와 톤 차이로 구분한 고밀도 카드
- 데스크톱 고정 사이드바와 상단 상태바
- 금융 숫자의 고정폭·tabular figures
- 상태 코드의 pill badge
- 실시간 변화를 위한 짧은 강조 효과
- 경고·위험 행동을 제한적으로 강조하는 색상 체계

수정할 콘셉트:

| 콘셉트 예시 | 실제 Cresta 적용 |
| --- | --- |
| 미국주식·선물·공매도 | 국내주식 KRX/NXT 지원 범위 |
| Interactive Brokers/CME | 키움증권 REST API |
| 실거래처럼 보이는 화면 | 모든 화면에 `키움 모의투자` 환경 표시 |
| API Key 직접 편집 | 자격증명 상태만 표시, 비밀값은 서버에서 관리 |
| Guard Master Switch | Guard는 비활성화 불가, 정책만 설정 |
| Full Auto 단일 토글 | 행동별 `AUTOMATIC/MANUAL_APPROVAL/DISABLED` |
| 감시 종목 4개 이상 | MVP 최대 3개 |
| 1·3·5분 분석 | 집중 5·10분, 일반 10·30분 기본 선택 |

| ID | 요구사항 |
| --- | --- |
| UI-001 | 기존 콘셉트의 색상·타이포그래피·고밀도 레이아웃을 시각 기준으로 사용한다. |
| UI-002 | 해외시장·타 증권사·공매도 예시는 실제 제품 화면에서 제거한다. |
| UI-003 | 환경, 거래소, 연결과 재동기화 상태를 모든 주요 화면에서 확인할 수 있어야 한다. |
| UI-004 | 색상만으로 손익·위험·상태를 구분하지 않고 텍스트·아이콘·코드를 함께 사용한다. |

### 3.2 전역 레이아웃과 내비게이션

상단 상태바:

```text
키움 모의투자 | KRX/NXT 세션 | Broker 연결 | 재동기화 상태
예수금 | 평가금액 | 당일 손익 | 알림 | 비상정지
```

사이드바:

```text
대시보드
감시 종목
보유 포지션
승인·주문
AI 판단
전략·설정
리스크
시스템 상태
이력·감사
```

| ID | 요구사항 |
| --- | --- |
| UI-010 | 데스크톱은 고정 사이드바와 상태바를 사용한다. |
| UI-011 | 태블릿은 사이드바를 접을 수 있고, 모바일은 하단 핵심 내비게이션과 추가 메뉴를 사용한다. |
| UI-012 | 환경 배지 `MOCK`을 항상 노출하고 실제 주문으로 오해할 표현을 사용하지 않는다. |
| UI-013 | `RECONCILING`, `DEGRADED`, `HALTED` 상태일 때 주문 가능 범위를 상태바에서 설명한다. |
| UI-014 | 비상정지는 모든 주요 화면에서 접근 가능하되 다른 버튼과 충분한 간격을 둔다. |

### 3.3 대시보드

필수 위젯:

- 계좌 평가금액·예수금·실현·미실현·당일 손익
- 현재 거래 세션과 신규매수 가능 종료까지 남은 시간
- 감시 종목 최대 3개와 최신 시세·데이터 시각
- 활성 포지션과 손절가·고점·수익률
- 최신 Scout/Core 판단과 실행 모드
- 승인 대기 주문과 남은 유효시간
- Guard 사용량: 총 투자한도, 일일 손실, 신규진입 횟수
- Broker·WebSocket·DB·재동기화 상태
- 실시간 주문·체결·위험 이벤트 타임라인

| ID | 요구사항 |
| --- | --- |
| UI-020 | 숫자마다 기준시각, 통화와 실현·미실현 구분을 표시한다. |
| UI-021 | AI 행동은 행동 코드, 근거, 신뢰도, 유효시간과 실행 모드를 함께 표시한다. |
| UI-022 | `Execute Order` 같은 일반 버튼 대신 승인 대상 가격범위·수량·Guard 결과가 포함된 승인 화면으로 연결한다. |
| UI-023 | 오래된 데이터는 마지막 값만 표시하지 않고 `STALE` 상태와 경과시간을 표시한다. |

### 3.4 감시 종목 및 전략 등록

등록 흐름:

```text
1. 종목 검색·선택
2. 거래 가능 시장과 NXT 지원 여부 확인
3. 진입 방식과 신규매수 시간 설정
4. 투자금·손절·목표수익·부분매도 설정
5. 행동별 자동·승인·비활성 설정
6. 분석 주기와 익일 보유 설정
7. 충돌 검증·영향 미리보기
8. 최종 확인 후 등록
```

| ID | 요구사항 |
| --- | --- |
| UI-030 | 감시 종목은 최대 3개까지만 등록할 수 있고 남은 슬롯 수를 표시한다. |
| UI-031 | 종목코드와 종목명을 함께 표시하고 자유 텍스트만으로 등록하지 않는다. |
| UI-032 | KRX·NXT 거래 가능 여부와 모의투자 주문 가능 시장을 구분해 표시한다. |
| UI-033 | `Full Auto` 단일 토글을 제공하지 않고 매수·부분매도·전량매도·손절 등 행동별 모드를 설정한다. |
| UI-034 | 사용자 기본값을 상속한 필드와 종목별 재정의 필드를 시각적으로 구분한다. |
| UI-035 | 저장 전에 모순 설정과 현재 계좌 한도 영향을 서버에서 검증한다. |

### 3.5 보유 포지션

포지션 카드·표에 표시할 항목:

- 종목코드·종목명·거래시장
- 현재가와 데이터 최신성
- 수량·매도 가능 수량·평균매입가
- 평가손익·수익률·당일 고점 대비 하락률
- 고정·추적 손절 상태와 현재 손절가
- Scout/Core 최신 행동·근거·유효시간
- 자동·승인 실행 모드
- 미체결 매도와 장 마감 청산 상태

사용자 행동:

- 손절선 조정
- 부분매도 요청
- 전량매도 요청
- 종목 긴급청산
- 포지션 상세·판단·주문 이력 열기

| ID | 요구사항 |
| --- | --- |
| UI-040 | 화면 버튼은 국내주식 MVP에서 지원하지 않는 공매도·Buy Cover를 제공하지 않는다. |
| UI-041 | 손절 변경이 즉시 청산 조건을 만들면 저장 전에 경고와 예상 행동을 표시한다. |
| UI-042 | 부분·전량매도는 실행 모드에 따라 즉시 Guard 검사 또는 승인 흐름으로 연결한다. |
| UI-043 | `UNKNOWN`, `RECONCILING`, 부분체결과 미체결 잔량을 숨기지 않는다. |

### 3.6 승인·주문

승인 카드 필수 정보:

- 종목·매수/매도·수량
- 판단 기준가격과 현재 호가
- 승인 허용 가격 상·하한
- 주문가격 정책과 재호가 횟수
- 예상 최대 금액·수수료
- 손절·목표수익·익일 보유 정책
- Core 판단 근거와 Guard 검사 결과
- 승인 만료까지 남은 시간
- 승인·거절 버튼

| ID | 요구사항 |
| --- | --- |
| UI-050 | 승인 만료, 가격범위 이탈 또는 데이터 지연 시 승인 버튼을 비활성화하고 원인을 표시한다. |
| UI-051 | 승인 직전에 서버의 최신 Guard 검사 결과를 다시 표시한다. |
| UI-052 | 승인 후 주문 상태를 `SUBMITTING`부터 체결·취소까지 실시간 표시한다. |
| UI-053 | 주문 결과가 `UNKNOWN`이면 실패나 재시도 버튼 대신 재동기화 상태를 표시한다. |
| UI-054 | 취소·정정·재주문 관계와 주문 그룹 전체 체결수량을 조회할 수 있어야 한다. |

### 3.7 전략·설정

설정 화면 영역:

- 거래 행동 실행 모드
- 감시·분석·신규매수 시간
- 주문가격·승인·미체결·재호가
- 익일 보유·동시호가·장 마감
- 투자·손실 한도
- 손절 전략
- 데이터·연결 위험
- 비상정지 기본 정책
- 알림
- 설정 버전·변경 이력

설정 편집 흐름:

```text
현재 활성 버전 조회
→ 초안 편집
→ 검증 및 영향 미리보기
→ 적용 시점 선택
→ 위험 변경 재인증
→ 활성화 결과 확인
```

| ID | 요구사항 |
| --- | --- |
| UI-060 | 설정 화면은 `Guard Master Switch`를 제공하지 않는다. |
| UI-061 | 변경 가능한 모든 정책과 변경 불가 무결성 상태를 명확히 구분한다. |
| UI-062 | API Key 원문·마스킹 값을 입력란으로 제공하지 않고 등록 상태·만료·연결 시험 결과만 표시한다. |
| UI-063 | 변경 전·후 차이, 경고·차단 오류와 현재 포지션 영향을 함께 표시한다. |
| UI-064 | 초안, 활성, 예약, 이전 버전과 롤백 결과를 조회할 수 있어야 한다. |

### 3.8 리스크 및 비상정지

비상정지 실행 모달:

```text
PAUSE_ENTRY
CANCEL_OPEN_ORDERS
EMERGENCY_LIQUIDATE
```

모달에는 영향 대상, 취소될 주문, 청산 대상 포지션, 실행 후 상태와 해제 조건을 표시한다.

| ID | 요구사항 |
| --- | --- |
| UI-070 | 비상정지 수준을 명확한 문장과 영향 범위로 설명한다. |
| UI-071 | `EMERGENCY_LIQUIDATE`는 확인 문구 입력 또는 동등한 강한 확인을 요구한다. |
| UI-072 | 실행 후 모든 화면에 활성 비상정지 수준과 원인·시각을 고정 표시한다. |
| UI-073 | 비상정지 해제는 별도 재인증·재동기화 흐름을 사용한다. |
| UI-074 | 비상정지 정책 편집과 실제 비상정지 실행을 같은 토글로 합치지 않는다. |

### 3.9 시스템 상태와 재동기화

표시 항목:

- 키움 환경·계좌 별칭·고정 IP 검증 결과
- REST·WebSocket·토큰·호출 제한 상태
- Active Broker worker와 lease
- PostgreSQL·Redis·worker 상태
- 마지막·진행 중 재동기화
- 미해결 주문·포지션 불일치
- 외부 주문·포지션 해결 작업

| ID | 요구사항 |
| --- | --- |
| UI-080 | 비밀정보 없이 키움 연결 상태와 토큰 만료 예정시간을 표시한다. |
| UI-081 | 재동기화 실행 단계, 소요시간, 불일치 코드와 거래 차단 범위를 표시한다. |
| UI-082 | 외부 주문·포지션은 수동 편입·취소 요청·청산 요청·격리 유지 선택지를 제공한다. |
| UI-083 | 시스템 로그는 비밀정보를 마스킹하고 사용자에게 필요한 구조화 이벤트를 우선 제공한다. |
| UI-084 | 개발용 터미널 명령 실행 기능은 제공하지 않는다. |

### 3.10 공통 화면 상태

모든 주요 화면은 다음 상태를 정의한다.

```text
LOADING | EMPTY | READY | STALE | DEGRADED | RECONCILING | HALTED | ERROR
```

| ID | 요구사항 |
| --- | --- |
| UI-090 | 빈 데이터와 로딩을 오류처럼 표시하지 않는다. |
| UI-091 | 연결 오류에서 마지막 정상 데이터와 발생시각을 구분한다. |
| UI-092 | 데이터가 오래됐을 때 사용자가 실행할 수 없는 행동과 이유를 가까운 위치에 표시한다. |
| UI-093 | 화면 갱신이 끊겨도 주문 성공·실패를 추정하지 않고 서버 스냅샷을 다시 조회한다. |

### 3.11 반응형·접근성

| ID | 요구사항 |
| --- | --- |
| UI-100 | 데스크톱 12열, 태블릿 6열, 모바일 단일열을 기본으로 한다. |
| UI-101 | 모바일에서는 승인·비상정지·포지션 위험을 차트보다 우선 배치한다. |
| UI-102 | 키보드만으로 내비게이션·설정·승인·비상정지 모달을 사용할 수 있어야 한다. |
| UI-103 | 포커스 상태, 오류 메시지, 필드 label과 접근 가능한 이름을 제공한다. |
| UI-104 | 실시간 가격 변화 애니메이션은 짧게 제한하고 감소된 움직임 설정을 지원한다. |
| UI-105 | 위험 색상은 WCAG 대비 기준을 만족하고 색상 외 텍스트 상태를 제공한다. |

## 4. 오류·예외 또는 경계 조건

- UI에서 성공 메시지를 표시하기 전에 서버의 활성 설정 버전 또는 주문 상태를 확인한다.
- 인증 실패 후 입력한 비밀번호와 TOTP를 화면에 다시 채우거나 자동 재전송하지 않는다.
- 세션 만료 후 기존 주문 승인 화면으로 돌아와도 사용자가 명시적으로 다시 확인하기 전에는 요청하지 않는다.
- 두 브라우저에서 설정을 동시에 수정하면 오래된 버전 저장을 거부하고 차이를 표시한다.
- 비상정지 중에도 화면 상태와 포지션 조회는 유지한다.
- 모바일 화면에서 위험 확인 정보를 생략해 버튼만 노출하지 않는다.
- 콘셉트 HTML에 포함된 비밀값 예시는 실제 UI·fixture·문서로 복사하지 않는다.
- 키움 모의투자에서 검증할 수 없는 NXT 주문 컨트롤은 비활성화하고 이유를 표시한다.

## 5. 검증·인수 조건

- 기존 콘셉트의 시각 언어를 유지하면서 국내주식·키움 모의투자 정보로 표시된다.
- 사용자 ID·비밀번호·TOTP를 모두 검증하지 않으면 보호된 화면에 접근할 수 없다.
- 모든 거래·Guard 정책을 Web UI에서 조회·편집하고 영향 미리보기를 확인할 수 있다.
- API Key·Secret·토큰 원문이 DOM, 화면, 로그에 노출되지 않는다.
- 행동별 실행 모드와 설정 출처·버전을 조회할 수 있다.
- 승인 만료·가격 이탈·데이터 지연 시 잘못된 주문 승인을 할 수 없다.
- `UNKNOWN`, 재동기화, 부분체결과 외부 주문 상태가 명확히 표시된다.
- 비상정지 수준별 영향과 해제 조건이 일관되게 표시된다.
- 데스크톱·태블릿·모바일과 키보드 접근성 검증을 통과한다.

## 6. 미결정·보류 항목

- 첫 버전 기본 언어는 한국어이며 다국어 전환은 제외한다.
- 차트는 서버의 1분봉 조회만 사용하고 tick 전체를 브라우저에 내려보내지 않는다. 라이브러리는 구현 전에 라이선스·접근성 검토 후 선택한다.
- 첫 버전 알림은 화면 내 알림과 운영 로그로 제한하고 브라우저·모바일 push는 제외한다.
- 위젯 배치는 고정하고 데스크톱의 정보 밀도 `COMPACT/COMFORTABLE`만 지원한다.
- 설정은 영역별 탭, 감시종목 등록은 단계별 마법사를 사용한다.
